[package]
name = "serde_ssml"
version = "0.1.0"
edition = "2024"

[dependencies]
chumsky = { version = "0.9.3" }
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
similar-asserts = "1.7.0"
